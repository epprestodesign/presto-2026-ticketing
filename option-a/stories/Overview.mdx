import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Aug 4 Changes/Overview" />

# Aug 4 Changes

The single overview for the **August 4 prototype work**. Three versions came out of that
round — **Option A** and **Option B**, two shapes of the same configure-then-book journey,
and **Option C**, the one-click grid built in response to the Aug 4 feedback on both.
Everything about all three is documented on this page. Component stories sit under
**Option A**, **Option B** and **Option C** in this same category.

A fourth, **Option D**, followed on Aug 5 and has its own
[*Aug 5 Changes*](?path=/docs/aug-5-changes-option-d-overview--docs) category.

All three are working copies of the Hotel + Packages experience, forked on
**August 4, 2026**; none of them modifies the design system.

> **There is a newer round.** [**Option D**](?path=/docs/aug-5-changes-option-d-overview--docs)
> is the **Aug 5** feedback response and the current direction — two identical packages, one
> per hotel, with the pair as the landing page. It lives under *Aug 5 Changes*. This page
> covers the Aug 4 round that led to it.
>
> **Within Aug 4, start with Option C.** A and B were built before that feedback landed. C is
> the answer to it: a 6-tile grid, no filtering, no modal, ticket free with the room. A and B
> are kept because their detailed configure-and-adjust UI is explicitly *"good for later, not
> the landing page"* — worth having when the package options expand.

<div style={{
  display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '16px',
  margin: '24px 0 28px',
}}>
  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-d/" target="_blank" rel="noreferrer"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #7C3AED, #01113E)', color: '#fff', borderRadius: '12px', padding: '20px 22px', textDecoration: 'none', minHeight: '150px', outline: '3px solid #7C3AED', outlineOffset: '2px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Option D · Aug 5 · newest</div>
    <div style={{ fontSize: '1.125rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Two hotels, one package</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Two identical packages, one per hotel — the pair IS the landing page. View the package, then check out.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-c/" target="_blank" rel="noreferrer"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '20px 22px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Option C · Aug 4</div>
    <div style={{ fontSize: '1.125rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>One-click packages</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>A 6-tile grid — 3 ticket tiers × 2 contracted hotels. Pick a tile, set the party size, check out. Ticket free with the room.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-a/" target="_blank" rel="noreferrer"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '20px 22px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Option A</div>
    <div style={{ fontSize: '1.125rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Packages first</div>
    {/* Keep the copy on one line: loose text on its own lines is parsed as a markdown
        paragraph and wrapped in `.sbdocs-p`, whose theme color overrides this white. */}
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Landing → Browse Packages → the full-screen package modal (tier · hotel · room · extras) → checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-b/" target="_blank" rel="noreferrer"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '20px 22px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Option B</div>
    <div style={{ fontSize: '1.125rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Hotel first</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>RSVP → two hotels → packages at that hotel, its room, or skip the stay. Three paths, one checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>
</div>

### The four versions

Option D is included here for comparison, though it belongs to the Aug 5 round and is
documented on its own [Option D overview](?path=/docs/aug-5-changes-option-d-overview--docs).

| | Option A | Option B | Option C | Option D |
| --- | --- | --- | --- | --- |
| **Round** | Aug 4 | Aug 4 | Aug 4 | **Aug 5** |
| **Shape** | Packages first | Hotel first | One-click grid | Two hotels, one package |
| **Live** | [/option-a/](https://epprestodesign.github.io/presto-2026-ticketing/option-a/) | [/option-b/](https://epprestodesign.github.io/presto-2026-ticketing/option-b/) | [/option-c/](https://epprestodesign.github.io/presto-2026-ticketing/option-c/) | [/option-d/](https://epprestodesign.github.io/presto-2026-ticketing/option-d/) |
| **Local dev** | http://localhost:6400 | http://localhost:6500 | http://localhost:6600 | http://localhost:6700 |
| **Source** | `option-a/` | `option-b/` | `option-c/` | `option-d/` |
| **Stories** | *Aug 4 Changes / Option A* | *Aug 4 Changes / Option B* | *Aug 4 Changes / Option C* | *Aug 5 Changes / Option D* |
| **Screens** | landing → packages → checkout | RSVP → hotels → packages → checkout | board → confirm → checkout | **packages → package details → checkout** |
| **Landing page** | hero + booking widget | hero + RSVP | the package board | **the package pair** |
| **Offerings** | 9 packages | 9, scoped to a hotel | 6 tiles (3 tiers × 2 hotels) | **2 packages, 1 per hotel** |
| **Filtering** | filter rail | filter rail | none | none |
| **Configuration** | full-screen modal (tier · hotel · room · extras) | full-screen modal | a confirm dialog; room choice at the premium hotel | none — the template page is the decision |
| **Party size** | on the package row | on the package row | one shared control above the board | one shared control in the header |
| **Dates drive price** | no | no | **yes** — nights multiply every room rate | no — a fixed two-night stay |
| **Library overrides + patches** | 4 + 4 | 4 + 4 | 0 + 0 | 0 + 0 |
| **Status** | the Aug 4 build | duplicate of A, carrying feedback | the Aug 4 **feedback response** | the Aug 5 **feedback response** |

A and B forked from
[`/experience-packages`](https://epprestodesign.github.io/presto-2026-ticketing/experience-packages/);
C forked from A, and D from C. Run any of them with
`cd <folder> && node ../node_modules/vite/bin/vite.js --port <port>`.

### Why Option C exists

The Aug 4 feedback asked for a grid over a scrolling list, packages above the fold, and a pure
one-click purchase with no filtering and no options — with the party size, capped by room
occupancy, as the only variable. Option A is the closer of the two starting points (it at least
opens on packages), but both render the library's Browse Packages page: a filter rail plus a
scrolling list, each row opening a configuration modal. Option C replaces that landing page
outright and keeps the rest of the journey.

Full detail is on the [Option C overview](?path=/docs/aug-4-changes-option-c-overview--docs).

**Deep links — Option D (two hotels, one package):**
[The pair (landing)](https://epprestodesign.github.io/presto-2026-ticketing/option-d/?screen=packages&people=5) ·
[Package template](https://epprestodesign.github.io/presto-2026-ticketing/option-d/?screen=packageDetails&pkg=ritz-package) ·
[On the Packages tab](https://epprestodesign.github.io/presto-2026-ticketing/option-d/?screen=packageDetails&pkg=ritz-package&tab=packages) ·
[Checkout](https://epprestodesign.github.io/presto-2026-ticketing/option-d/?screen=checkout&pkg=ritz-package&people=4) ·
[Hotel details](https://epprestodesign.github.io/presto-2026-ticketing/option-d/?screen=hotelDetails&hotel=westin)

**Deep links — Option C (one-click grid).** Every step is shareable, including the
stay itself, because dates and party size change every price on the board:

[The board](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=packages) ·
[3 nights, 6 guests](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=packages&from=2026/09/18&to=2026/09/21&guests=6) ·
[Choose your room (Ritz)](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=packages&open=club-ritz) ·
[Confirm only (Courtyard)](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=packages&open=mezz-courtyard) ·
[A room upgrade picked](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=packages&open=club-ritz&roomType=executive-suite) ·
[Hotel Details (read-only)](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=hotelDetails&hotel=ritz) ·
[Checkout](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=checkout&pkg=club-ritz&guests=4) ·
[Confirmation](https://epprestodesign.github.io/presto-2026-ticketing/option-c/?screen=confirmation&pkg=club-ritz)

**Deep links — Option A (packages first):**
[Landing](https://epprestodesign.github.io/presto-2026-ticketing/option-a/?screen=landing) ·
[Browse Packages](https://epprestodesign.github.io/presto-2026-ticketing/option-a/?screen=packages) ·
[Package modal](https://epprestodesign.github.io/presto-2026-ticketing/option-a/?screen=packages&pkg=grid-0-legends-tour) ·
[Checkout](https://epprestodesign.github.io/presto-2026-ticketing/option-a/?screen=checkout) ·
[Confirmation](https://epprestodesign.github.io/presto-2026-ticketing/option-a/?screen=confirmation)

**Deep links — Option B (hotel first):**
[Landing](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=landing) ·
[Hotel selection](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=hotels) ·
[Packages at a hotel](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=packages&hotelId=courtyard&room=courtyard) ·
[Skip the hotel](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=packages&noHotel=1) ·
[Hotel details](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=hotelDetails&hotelId=westin) ·
[Checkout](https://epprestodesign.github.io/presto-2026-ticketing/option-b/?screen=checkout)

These components live in the prototype, **not** in the design system. Storybook
documents them here so the work is reviewable in one place before any of it is
promoted into `src/`.

## What changed

**The event.** Pittsburgh Steelers at New England Patriots · Sun, Sep 20, 2026 ·
1:00 PM · Gillette Stadium, replacing the library's Patriots v Bills sample
throughout — hero imagery, browse headers, packages, checkout and confirmation.

**The flow.** A and B both drop the Package Details *page* — everything about a
package lives in a full-screen modal — and differ in what you decide first. C
drops the modal too, and asks you to decide almost nothing.

**Option D — two hotels, one package** (the Aug 5 feedback response, newest):

```
Packages ──▶ Package details ──▶ Checkout ──▶ Confirmation
(landing)      library template
 two tiles,     packages pulled to the top
 one per hotel        │
      │               └─ Select ──▶ Checkout
      └─ hotel NAME ──▶ Hotel Details (new tab, informational)
```

The package pair IS the landing page — no hero, no search band, no headcount gate.
Both packages carry identical inclusions and are named for the only thing that
separates them: **Westin Package** and **Ritz-Carlton Package**. One variable, the
party size, sits in the header. The stay is a fixed two nights. Stepper:
**Package · Review**, hidden on the landing page.

**Option C — one-click grid** (the Aug 4 feedback response):

```
                              ┌─ Ritz-Carlton ─▶ [choose your room]  ─┐
Package board ──▶ Select ─────┤                                       ├──▶ Checkout ──▶ Confirmation
   (landing)                  └─ Courtyard ────▶ [confirm package]  ──┘
      │
      ├─ dates + guests ─────▶ re-prices all six tiles live
      └─ hotel NAME ─────────▶ Hotel Details (new tab, read-only)
```

The board **is** the landing page — no hero-then-search step, so packages are the
first thing on screen. Six tiles: 3 ticket tiers × 2 contracted hotels, one row
per property.

Selecting a tile raises **one dialog that does one of two jobs**, decided by the
hotel: the Ritz-Carlton carries five contracted room types so it offers the
choice; the Courtyard carries one so it is pure confirmation of the final
package. Either way it *confirms* rather than *configures* — the tier, the hotel
and the free-ticket model stay baked into the tile.

Dates and guests live in one booking widget above the board (Option A's widget
fork, minus the Search button) because both re-price all six tiles at once.
Nights multiply the room and nothing else; occupancy caps tickets per room, and a
larger party books a second room rather than being refused.

Stepper: **Packages · Review**, hidden on the board itself — before anything is
selected it is a progress bar for progress not yet made.

**Option A — packages first:**

```
Landing ──▶ Browse Packages ──▶ [package modal] ──▶ Checkout ──▶ Confirmation
                                      │
                                      └─▶ Hotel Details (new tab, read-only)
```

Stepper: **Packages · Review**. Browse Hotels sits outside the journey; Hotel
Details opens in a new tab as a read-only reference with every booking CTA
suppressed.

**Option B — hotel first:**

```
                    ┌─ Select a package ─▶ Packages (scoped to that hotel) ─┐
RSVP ──▶ Hotels ────┼─ hotel NAME ───────▶ Hotel Details (one room) ────────┼─▶ Checkout ──▶ Confirmation
                    └─ Skip / no hotel ──▶ Packages (tickets only) ─────────┘
```

Stepper: **Hotel · Package · Book**. Step 2 uses the Browse Hotels results layout
trimmed to the two hotels this event holds, with a compact event bar in place of
the page hero so both cards clear the fold.


**Two shapes of package.** Of nine SKUs, five include a stay and four are
package-only. A row shows its rooms line only when there is one.

## Live pricing

**Options A and B — the bundle model.** Nothing about a package is fixed. Tier,
party size, hotel, room type and paid extras all re-price it, following the
library's own convention:

```
components = tier.price × guests  +  room.nightly × nights  +  experience value
package    = round(components × (1 − bundle discount rate))
total      = package + paid extras          ← add-ons sit outside the bundle
```

So *Legends Stadium Tour* is **$604** in Upper Level for two, **$1,074** in Club
Level for two, and **$2,929** in Club Level for eight. The same numbers flow to the
cart, checkout and confirmation.

**Option C — the free-ticket model.** The pitch is inverted: you are not buying a
discounted bundle, you are buying a **room** and the tickets come with it.

```
rooms   = ceil(guests / room occupancy)     one room only sleeps so many
price   = nightly × nights × rooms          ← what's charged
value   = price + ticket face × guests      ← struck through
saving  = ticket face × guests              ← "N tickets free"
```

Nights multiply the room and **nothing else**, so a three-night stay triples the
price and leaves the saving untouched. The nightly rate is the contracted rate
plus a tier uplift of roughly half the ticket's face value — which is why the
same hotel costs more with a Club Level ticket than a Mezzanine one.

| | Club Level | Lower Level | Mezzanine |
| --- | --- | --- | --- |
| **The Ritz-Carlton** · Carlton Suite · sleeps 4 | $809 | $729 | $639 |
| **Courtyard by Marriott** · Double Queen · sleeps 4 | $479 | $399 | $309 |

*One night, two guests. The board re-prices from the dates and party size above it.*

**Option D — the package model.** Neither a bundle discount on a configurable package nor a
free ticket: a fixed package, priced for the party, at one of two hotels.

```
rooms      = ceil(people / room occupancy)
components = ticket face × people  +  nightly × 2 nights × rooms
             +  transportation × rooms  +  hospitality × people
package    = round(components × (1 − 12% bundle discount))
```

There are no dates in that formula because the stay is fixed at two nights. Occupancy is
the thing to watch: the Westin's room sleeps 2 and the Ritz's sleeps 4, so the cheaper
hotel is not always the cheaper package.

## How the library stays untouched

No prototype here edits `src/`. Options A and B use two mechanisms in their
`vite.config.js`, in that app's build only:

| Mechanism | Used for |
| --- | --- |
| **`resolveId` overrides** | Redirect a library file to a local fork wherever it's imported — `BookingWidget`, `PackageResultCard`, `PackageQuickViewDialog`, and the shared hero image. |
| **Source patches** | Surgical find/replace on read, for the handful of spots that aren't prop-driven: the Browse Hotels event header, the "Who's Attending?" heading, two-way Guests on the packages page, `maximized` + slide-up on the dialog, and a scroll-snap fix in the room carousel. |

A patch that no longer matches **fails the build loudly** rather than silently
rendering the wrong thing.

**Options C and D need neither.** Their `OVERRIDES` and `PATCHES` are both empty:

| | Option A | Option C | Option D |
| --- | --- | --- | --- |
| Library component overrides | 4 | **0** | **0** |
| Library source patches | 4 | **0** | **0** |

A and B render the library's Browse Packages *page* and have to reshape its
parts. C and D render their own screens out of their own components, so there is
no library page to bend — and their builds cannot break when the library moves.
The library components they mount are all used as shipped: C uses `GlobalNav`,
`AppStepper`, `DateRangeCalendar`, `HotelDetailPage`, `CheckoutPage` and
`ConfirmationPage`; D swaps the calendar for **`PackageDetailPage`**, which is
its screen 2 outright.

That last one matters: **every hotel-details link in Option C opens the library's
own `Hotel Details / Book Reservation` page template**, in a new tab, read-only —
the template's Reserve Room CTAs, back-to-results link and search band suppressed,
with a banner saying why.

## What's documented here

**Option A** and **Option B** each carry the same set:

- **Package Details** — the modal and every block inside it, plus the browse row.
- **Booking Widget** — the forked widget (Guests dropdown, gameday dates).
- **Foundations** — the reusable carousel used in three places.

**Option C** carries its own set, plus its own overview doc — because what it
*removes* from Option A is the substance of it:

| Story | What it documents |
| --- | --- |
| [Overview](?path=/docs/aug-4-changes-option-c-overview--docs) | Each feedback point and how it's answered, the free-ticket pricing model, and what C removes from A |
| [Package Grid](?path=/story/aug-4-changes-option-c-package-grid--six-tiles) | The 6-tile board, plus 3-night and large-party states and a 9-tile density study |
| [Package Tile](?path=/story/aug-4-changes-option-c-package-tile--premium-stay) | One complete SKU — room priced, tickets free, rooms derived from occupancy |
| [Package Confirm Dialog](?path=/story/aug-4-changes-option-c-package-confirm-dialog--choose-your-room) | The two modes side by side: choose-your-room at the Ritz, confirm-only at the Courtyard |
| [Event Hero](?path=/story/aug-4-changes-option-c-event-hero--default) | The branded hero, with the search card tucked onto it |
| [Booking Widget](?path=/story/aug-4-changes-option-c-booking-widget--core-widget) | Dates + guests, no Search button — with Option A's original beside it for comparison |
| [Event Header Bar](?path=/story/aug-4-changes-option-c-event-header-bar--default) | The ~90px alternative to the hero, kept for the above-the-fold trade-off |
| [Hotel Details](?path=/story/aug-4-changes-option-c-hotel-details--ritz-carlton) | The library page template in read-only mode |

**Option D** has its own category and overview under
[*Aug 5 Changes*](?path=/docs/aug-5-changes-option-d-overview--docs):

| Story | What it documents |
| --- | --- |
| [Overview](?path=/docs/aug-5-changes-option-d-overview--docs) | Each Aug 5 feedback point and how it's answered, the pricing model, and what D removes from C |
| [Package Pair](?path=/story/aug-5-changes-option-d-package-pair--two-hotels) | The two tiles — identical contents, named for their hotel |
| [Package Details](?path=/story/aug-5-changes-option-d-package-details--ritz-carlton) | The library package template, with its packages section pulled to the top |
| [Hotel Details](?path=/story/aug-5-changes-option-d-hotel-details--westin) | The library hotel template in read-only mode |

### One open trade-off

The 300px branded hero pushes the first tile row just below the fold on a laptop,
which pulls against the feedback's *"packages visible without scrolling"*.
`EventHeaderBar` (~90px, no artwork) is kept and documented as the alternative —
swapping them is one line in `PackageGridScreen.vue`. Worth a decision at review.
