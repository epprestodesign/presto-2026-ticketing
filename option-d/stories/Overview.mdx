import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Aug 5 Changes/Option D/Overview" />

# Option D — Two hotels, one package

The **August 5 feedback response**. Option C answered the Aug 4 feedback with a six-tile
board; Aug 5 cut that down to the single decision that actually matters for a pre-invited
group.

<div style={{
  display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '16px',
  margin: '24px 0 28px',
}}>
  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-d/" target="_blank" rel="noreferrer"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #7C3AED, #01113E)', color: '#fff', borderRadius: '12px', padding: '20px 22px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Option D · newest</div>
    {/* Keep the copy on one line: loose text on its own lines is parsed as a markdown
        paragraph and wrapped in `.sbdocs-p`, whose theme color overrides this white. */}
    <div style={{ fontSize: '1.125rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Two hotels, one package</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Two identical packages, one per hotel — the board is the landing page. Set your party size on a card and check out from it, or drill into the detail page first. No dates, no tiers, no filters.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>
</div>

## The flow

```
Packages ─── Select & Check Out ────────────▶ Checkout ──▶ Confirmation
(landing)                                        ▲  ▲
 two tiles,                                      │  │
 one per hotel ─ View package details ──▶ Package details ─ Select ─┘
      │                                    the library's template  │
      └─ hotel NAME ──▶ Hotel Details ─ Select package ────────────┘
                        (new tab)
```

**The packages page is the landing page**, and it can also be the last page before
checkout. There is no headcount screen in front of it — the party size sits on each card,
above the price it drives — and no stepper above it, because a progress bar over a landing
page is orientation for progress not yet made. The stepper appears once a package has been
opened or chosen.

**Two ways out of a card.** `Select & Check Out` goes straight to Review.
`View package details` opens the library's own `PackageDetailPage`, and that page's Select
CTA continues to the same place — a longer route through the decision, not a gate in front
of it.

Hotel details is still **not a step** — it opens in its own tab from any mention of a hotel,
so the screen behind it keeps its state. It is no longer a *dead end*, though: its room card
selects the package too, and selecting there continues in that tab.

## The Aug 5 afternoon revision

| Feedback | Answer |
| --- | --- |
| Party size belongs **on the card**, not at page level | Moved onto both cards, above the price. Still one number — both controls bind to the same store value, so setting it on either re-prices both. |
| Don't make me open a second page to select and check out | `Select & Check Out` on the card goes straight to Review. |
| Keep the drill-in for when I *do* want the detail | `View package details` beside it, and that page is unchanged. |
| On a hotel page, make `Price Details` a **Select package** action | The room card under *Room types at this property* now selects that hotel's package — same party-size control, same price block, same rhythm as the package cards. Both hotels behave identically. |

On the hotel page the headline is the **package total, all in** rather than the room rate,
because that is what checkout charges — a `$578` button landing on a `$3,091` checkout would
read as a bait-and-switch. `Price details` survives as the secondary link, rewritten to break
down the *package*: the four inclusions, then the bundle discount.

## The Aug 6 revision

| Feedback | Answer |
| --- | --- |
| "View package details" should show **only the package I picked** | The detail page carries one package. It used to show both with the clicked one leading — which meant the button opened something other than what was asked for, re-opening a decision already made. The board is one click away for comparing. |
| Checkout should be **one form**, not a Next button revealing each section | Checkout mounts `CheckoutPageExpanded`: Contact · Payment · Review your order · Policies, all open, one **Book Now**. The rail is untouched. |

`CheckoutPageExpanded` is a library component, ported from presto-2026 and extended with
this fork's `ticketing` mode. All six of its flows are documented under
[Checkout Experience Expanded](?path=/docs/checkout-experience-expanded-overview--docs) —
**Packages + Hotel** is the one Option D mounts.

Each card also states what the party size bought *there* — `7 tickets · 4 rooms` at the
Westin against `7 tickets · 2 rooms` at the Ritz — because occupancy differs, so the same
party is a different room count on each. That is the line that keeps the two cards from
looking like they disagree now that both carry the control.

## What the feedback asked for

| Feedback | How Option D answers it |
| --- | --- |
| Landing looks too much like Presto | No hero, no search band — the landing page is the package pair under a compact event strip. |
| Remove the check-in/check-out/guests bar | No booking widget on any screen. The stay is fixed. |
| A simple "how many people are coming" prompt | A party-size control on each package card, above the price it drives. |
| Pre-invited for two days — keep it minimal | The stay is a fixed two nights; nothing offers to change it. |
| Packages page above the fold | A ~90px event strip, then the tiles. It is now screen **1**. |
| Two tiles side by side only, one per hotel | Exactly two, in a fixed 2-column track. |
| Contents identical; only the hotel differs | Same tickets, transportation, hospitality and nights on both. |
| Name each by hotel | **Westin Package** · **Ritz-Carlton Package** |
| Each includes two tickets, transportation, etc. | Tickets scale with the headcount; transportation and hospitality are in both. |

## Why there is no search band

A booking widget — even trimmed to one field — reads as **search**: it implies something to
search for, and results that might come back empty. This flow has neither. The party is
already invited, the dates are already set, and there are exactly two options waiting.

So the check-in/check-out/guests bar wasn't shrunk, it was removed. What survives of it is
a single party-size select, sitting on the card it prices rather than gating the page in
front of it.

## Why both cards say the same words

The inclusion list is word-for-word identical on both packages, on purpose. The repetition
is what makes *"these are the same package, in two places"* legible at a glance — anything
phrased differently between them would imply a distinction that isn't there.

What genuinely differs is the hotel, the price, and one thing worth watching:

| | The Westin | The Ritz-Carlton |
| --- | --- | --- |
| Room | Deluxe King · sleeps 2 | Carlton Suite · sleeps 4 |
| Rooms for a party of 4 | **2** | **1** |

Occupancy means the cheaper hotel is not always the cheaper package. Each card states its
own room count rather than letting the price move unexplained.

## How a package is priced

```
rooms      = ceil(people / room occupancy)
tickets    = ticket face value × people
stay       = nightly rate × 2 nights × rooms
extras     = transportation × rooms  +  hospitality × people
components = tickets + stay + extras
package    = round(components × (1 − 12% bundle discount))
```

Prototype economics, deterministic so demos never drift.

## What Option D removes from Option C

| | Option C | Option D |
| --- | --- | --- |
| Tiles | 6 (3 tiers × 2 hotels) | **2** (1 per hotel) |
| Ticket tier | a choice, 3 ways | fixed — one tier |
| Dates | a picker; nights drive price | **fixed** two nights, no picker |
| Room choice | 5 room types at the premium hotel | one room per hotel |
| Confirm dialog | choose-your-room / confirm | none — the card checks out, and the detail page is optional |
| Library overrides + patches | 0 + 0 | **0 + 0** |

Like Option C, Option D touches no library file: its `OVERRIDES` and `PATCHES` are both
empty. The library components it mounts — `GlobalNav`, `AppStepper`, **`PackageDetailPage`**,
`HotelDetailPage`, `CheckoutPage`, `ConfirmationPage` — are all used as shipped. Screen 2 is
that package template, unmodified.

## Deep links

```
?screen=packages&people=5                              screen 1, party of five
?screen=packageDetails&pkg=ritz-package                screen 2, the package template
?screen=packageDetails&pkg=ritz-package&tab=packages   screen 2, on the Packages tab
?screen=checkout&pkg=ritz-package&people=4             screen 3
?screen=hotelDetails&hotel=westin                      the informational tab
```

## Still open

Option C is unchanged and still deployed — the Aug 5 feedback said to keep the current
concept as-is. Nothing has been archived yet; worth deciding once D is reviewed beside it.
