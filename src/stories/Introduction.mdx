import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Getting Started/Introduction" />

# Presto Design System

A **themed [Quasar](https://quasar.dev) component library** for an
**event travel — hotel booking & reservation** experience (EventPipe / Presto
branding), documented and QA'd in Storybook.

Every example, label, and edge case is written around one real journey: a company
or family traveling to a game — searching hotels near the venue, comparing
rooms, **reserving a single stay**, **holding a room block for a group/company**, or
**booking several rooms at once**, then paying (when applicable), confirming, and
managing the booking afterward. Throughout, the scenario is an **EventPipe
client-appreciation outing** at **Gillette Stadium, Foxborough, MA**.

Two reference events are in play, and it is worth knowing which you are looking at:

| Event | Used by |
| --- | --- |
| **New England Patriots v Buffalo Bills** · Sat, Dec 6, 2026 | The design system itself and the original prototypes — Booking Journey, Ticket + Hotel Bundle, Hotel + Tickets, Hotel + Packages |
| **Pittsburgh Steelers at New England Patriots** · Sun, Sep 20, 2026 · 1:00 PM | The **Aug 4** prototypes — Options A, B and C |

The Aug 4 options swap the event at their own boundary — each has a local
`event.js` — so the library keeps its own sample data untouched. A and B also
patch a couple of library pages that print the event from module constants rather
than props; Option C renders its own board, so it needs no patch at all. Same
venue, same journey, same components — only the matchup and date differ.

<div style={{ fontSize: '1.125rem', fontWeight: 700, margin: '28px 0 4px' }}>▶ Try the interactive prototypes</div>
<div style={{ marginBottom: '16px', maxWidth: '78ch', color: 'var(--ds-color-text-subtle, #5a5f6b)' }}>
  Clickable, end-to-end journeys built entirely from the real library components (zero forks). Each launches in a new tab.
</div>

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: '16px', margin: '0 0 28px' }}>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/experience/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #1D4ED8, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Stepper journey</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Hotel + Tickets</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Stays → Tickets → Review: browse hotels, pick seats on the Ticket Map, then a Tickets + Hotel checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/experience-packages/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #1D4ED8, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Stepper journey</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Hotel + Packages</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Stays → Packages → Review: browse hotels, choose an experience package, then a Packages + Hotel checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/prototype/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #2563EB, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Hotel booking</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Booking Journey</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>All three modes — Book Reservation, Group Block, Multiple Reservations — with a live map, filters, rooms, checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-c/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px', outline: '3px solid #0F766E', outlineOffset: '2px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Aug 4 · Option C · newest</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>One-click packages</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>A 6-tile grid — 3 ticket tiers × 2 contracted hotels. Pick a tile, set the party size, check out. Ticket free with the room.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-a/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Aug 4 · Option A</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Packages first</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>Landing → Browse Packages → a full-screen package modal (tier · hotel · room · extras) → checkout. Live pricing throughout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

  <a href="https://epprestodesign.github.io/presto-2026-ticketing/option-b/" target="_blank" rel="noopener"
     style={{ display: 'flex', flexDirection: 'column', background: 'linear-gradient(160deg, #0F766E, #01113E)', color: '#fff', borderRadius: '12px', padding: '18px 20px', textDecoration: 'none', minHeight: '150px' }}>
    <div style={{ fontSize: '0.75rem', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', opacity: 0.8 }}>Aug 4 · Option B</div>
    <div style={{ fontSize: '1.0625rem', fontWeight: 800, marginTop: '4px', color: '#fff' }}>Hotel first</div>
    <div style={{ fontSize: '0.875rem', marginTop: '6px', color: '#fff', opacity: 0.92, flex: 1 }}>RSVP → two hotels → packages at that hotel, its room, or skip the stay entirely. Three paths, one checkout.</div>
    <div style={{ fontWeight: 700, marginTop: '12px', color: '#fff' }}>Launch ↗</div>
  </a>

</div>

<div style={{ marginBottom: '24px' }}>
  <a href="https://epprestodesign.github.io/presto-2026-ticketing/hub/" target="_blank" rel="noopener" style={{ fontWeight: 700 }}>→ Experience Hub</a>
  <span style={{ color: 'var(--ds-color-text-subtle, #5a5f6b)' }}> — a landing directory linking every prototype.</span>
  <div style={{ marginTop: '6px' }}>
    <a href="/?path=/docs/aug-4-changes-overview--docs" style={{ fontWeight: 700 }}>→ Aug 4 Changes</a>
    <span style={{ color: 'var(--ds-color-text-subtle, #5a5f6b)' }}> — what Options A, B and C change, and every component behind them.</span>
  </div>
  <div style={{ marginTop: '6px' }}>
    <a href="/?path=/docs/aug-4-changes-option-c-overview--docs" style={{ fontWeight: 700 }}>→ Aug 4 · Option C</a>
    <span style={{ color: 'var(--ds-color-text-subtle, #5a5f6b)' }}> — the one-click package grid built from the Aug 4 feedback. Start here.</span>
  </div>
</div>

## ⭐ Current work — Option C, the one-click package board

The newest prototype, and the response to the **August 4 feedback** on Options A and B.
Where those two lead with the library's Browse Packages page — a filter rail plus a
scrolling list of rows, each opening a configuration modal — Option C replaces the
landing page outright:

```
                              ┌─ Ritz-Carlton ─▶ [choose your room]  ─┐
Package board ──▶ Select ─────┤                                       ├──▶ Checkout ──▶ Confirmation
   (landing)                  └─ Courtyard ────▶ [confirm package]  ──┘
      │
      ├─ dates + guests ─────▶ re-prices all six tiles live
      └─ hotel NAME ─────────▶ Hotel Details (new tab, read-only)
```

- **The board is the landing page.** Six tiles — 3 ticket tiers × 2 contracted hotels,
  one row per property. A tile is a complete SKU; no filtering, no result list.
- **"Get your ticket free, stay at this hotel."** The price charged is the **room**;
  tickets are $0 and shown as the saving. Nights multiply the room and nothing else.
- **One shared control for dates and guests**, above the board, because both re-price
  all six tiles at once. Occupancy caps tickets per room; a bigger party takes a
  second room.
- **Selecting a tile confirms rather than configures.** One dialog, two jobs: the
  Ritz-Carlton carries five room types so it offers the choice; the Courtyard carries
  one so it is pure confirmation.
- **Hotel names open the library's own `Hotel Details / Book Reservation` page**
  in a new tab, read-only, so the board keeps its dates and party size.

It is also the cleanest example in this repo of a prototype that **touches nothing**:
zero library component overrides and zero source patches, against four of each in
Option A. Full detail on the
[Option C overview](/?path=/docs/aug-4-changes-option-c-overview--docs); the flow for
all three sits on [Aug 4 Changes](/?path=/docs/aug-4-changes-overview--docs).

> **Building the localized prototype or a full user journey?** Read
> **Getting Started → User Journey & Build Spec** — it specifies every stage of
> every flow (all three booking modes) and its edge cases. Read **Architecture &
> Conventions** for the machine-oriented map (task → file, conventions, and the
> shared components to reuse instead of rebuild). The **interactive prototype**
> above (source in `prototype/`) is a working reference implementation of all
> three flows.

---

## The three booking modes (learn these first)

Three modes run through every flow and are named on nearly every component.
**The concept is stable; the exact prop name/value differs by component** — see
the cheat-sheet in **Architecture & Conventions** before wiring anything.

| Mode | Plain meaning | Charged? | Seen as |
| --- | --- | --- | --- |
| **reserve** | One **single stay** — one hotel, one check-in/out, one guest party. | Yes — credit card at checkout. | `reserve` / `reservation` |
| **hold** | **Hold a block of rooms** for a group or team — multiple hotels, rooms held **per night** via steppers, optional teams block. | No — the organizer holds; attendees pay later. | `hold` / `group` |
| **reservations** | **Multiple room reservations** at once — several hotels/rooms booked together, per-hotel guest info, one checkout. | Yes — credit card at checkout. | `reservations` |

The single biggest source of bugs is mode-name drift across components
(`reserve` vs `reservation`, `hold` vs `group`). The Build Spec and Architecture
doc both carry the canonical mapping.

---

## How to read this library

The sidebar is organized the way product and design think, not alphabetically.
It has **two layers**:

**1. Primitives** — the design-system building blocks, all under one
**Components** parent: **Actions, Forms, Feedback & Status, Layout & Structure,
Media & Visuals, Typography & Content** (plus **Foundations** for the raw tokens).

**2. Experience flows** — the booking journey, in order:

`Landing Page` → `Browse Hotels` → `Hotel Details` → `Checkout Experience` → `Confirmation` → `Manage Booking`

Every experience flow follows the **same shape**: a top-level **Book Reservation**
and **Group Block** story (the composed page for that mode) plus a **Components**
folder holding the pieces that page is built from. **App Shell** holds the chrome
(Global Nav & Cart, Page Frame) shared by all flows.

---

## Sidebar map

| Category | What it's for |
| --- | --- |
| **Getting Started** | Introduction (this page), Architecture & Conventions, User Journey & Build Spec. |
| **Foundations** | Design tokens made visible — colors, palette, type, icons, imagery, spacing, radius, elevation, breakpoints, motion. |
| **Components → Actions** | Button, Dialog, **Modal** (`DsModal`), **Side Panel** (`DsSidePanel`), Backdrop. |
| **Components → Forms** | Text/Area/Select/Autocomplete, Checkbox + **Checkbox Tree**, Radio, **Choice Chips**, Switch, Date/Time Picker, Slider, **Range**, Rating, **Quantity Stepper**, **Phone Field**, Transfer List. |
| **Components → Feedback & Status** | Alert, Banner, Badge, Snackbar, **Toast**, Progress, Skeleton. |
| **Components → Layout & Structure** | Box, **Card** (`DsCard`), Container, Grid, Stack, Divider, Section Header, List, **List Item**, **Accordion**, Tabs, Table. |
| **Components → Media & Visuals** | Avatar, Icon, **Image List**, Display Ad, Hero Banner, Rating. |
| **Components → Typography & Content** | Typography, Chip, **Amenity**, Tooltip. |
| **App Shell** | **Global Nav & Cart** (Book Reservation / Group Block variants) and **Page Frame** (the nav + content + footer shell). |
| **Landing Page** | The event landing page (Book Reservation / Group Block) + its Booking Widgets (Core / Teams). |
| **Browse Hotels** | Discovery — booking widget, filters (+ "All Filters" modal), listing cards, map (search-radius), availability, result states. |
| **Hotel Details** | The chosen hotel — photo gallery, summary header, detail tabs, room selection (rooms grid + room cards), amenities, policies. |
| **Checkout Experience** | The stepped checkout — review order, contact/group info, payment, review reservation; Old Designs holds the retired payment dialogs. |
| **Confirmation** | Post-booking success page (Book Reservation / Group Block). |
| **Manage Booking** | The account page (pinned last) — profile, communications, payment, security, help. |

---

## Experience flows

### Landing Page
**The event entry point.** Global nav → hero (event name + dates) → **Booking
Widget** → event description / who's attending / venue info → display ads →
footer. `mode` picks the widget + cart behavior: **Group Block** shows the
**Teams Booking Widget** (registered teams + rooms-needed); **Book Reservation**
shows the **Core Booking Widget** (travelers + rooms). No cart badge here —
nothing has been added yet.

### Browse Hotels
**Discover and choose where to stay.** Search entry, filter, compare on cards or
a map, and start a booking.

- **Hotel Listing Card** — the core **horizontal** result card (vertical is a
  legacy variant kept for reference). **Book Reservations** cards CTA **"Choose
  Your Room"**; **Group Block** cards CTA **"Select Rooms"**. Each has three
  availability edge cases and an expandable **Availability** panel
  (`RoomAvailability` — per-room, per-night rooms-left).
- **Result States** — Full / Partial / No Results / Error / Loading, with firm
  section separators between availability blocks (matches → doesn't-match-filters
  → no-availability). Exact copy is in the Build Spec.
- **Hotel Map** — price-pill markers, clustering, native +/- zoom, and a
  **search-radius** circle two-way bound to a slider (Google Maps, graceful
  no-key fallback).
- **Search & Filters** — one `Filter` component for every filter type (amenities
  with inline search, room-type chips ordered least→max occupancy, ratings,
  brands tree, budget with per-night ⇄ total toggle + histogram). An **All
  Filters** modal consolidates them with a live "Show N results" count.
- Pagination exists as a primitive (**Components → Footer → Pagination**) but the
  composed browse pages **no longer paginate** — results render as one list.

### Hotel Details
**The chosen hotel, end to end.** `HotelDetailPage` composes: optional booking
widget band → back link → **Photo Gallery** (mosaic + "see all" modal) → sticky
**Detail Tabs** (Overview / Rooms / Property / Amenities / Policies) → **Summary
Header** → About → **Rooms** → Amenities → Policies. `roomsFlow` (`reserve` /
`group`) selects the room card + widget mode.

- **Rooms** — the "Select Your Room" section is a **responsive grid** (not a
  carousel): up to 3 cards per row, wrapping. Sold-out rooms stay visible and
  disabled, never removed.
- **Room Card** — no room-type image, no amenities list. Header (room type / bed
  config / occupancy) + per-night rooms-left + price + CTA. Sold-out shows a red
  **"Sold Out"** night, a disabled **"Unavailable"** button, and a note.

### Checkout Experience
**Turn a selection into a booking.** An Airbnb-style "Confirm and pay" — a
stepped accordion on the left, a sticky order-summary rail on the right, and a
minimal centered **"Secure Checkout"** nav (no Manage Booking / cart). A **"Time
left to book"** timer sits under the price details.

- **Book Reservation** (`reserve`) & **Multiple Room Reservations**
  (`reservations`): **Contact info → Payment (credit card only) → Review
  reservation** (trip protection + cancellation + policies). Rail shows the order
  + Edit reservation / Start over.
- **Group Block** (`hold`): **Review order → Contact & group info → Review
  reservation**. **No payment, no protection, no totals/taxes** — nightly cost
  only. Requires a **Group Block Name**, a **mandatory Organization**, supports
  **additional emails**, and a **single** policies checkbox for all hotels.
- **Old Designs** holds the retired Instacart-style payment dialogs (superseded
  by the inline `PaymentForm`).

### Confirmation
**The post-booking success page.** Redesigned to match production: a success
**banner** → **Summary** (booking/group ID, release date, action buttons —
Book in Block (hold) / Copy Booking Link / Print — meta grid, and per-hotel rooms
held/booked per night) → per-hotel **Policies**. The older post-booking modules
(rating, who's-going, calendar, live map, area tips, FAQ, help) were removed.

### Manage Booking
**The account home** (pinned last). Two-column page: avatar + name + Edit profile
on the left; section content on the right — **Profile**, **Communications**,
**Payment methods**, **Security**, **Help & feedback**.

---

## Where to customize

| What you want to change | File |
| --- | --- |
| Brand colors, base font, global radius | `src/css/quasar.variables.scss` |
| Design tokens (palette, roles, spacing, radius, type) | `src/css/ds-palette.scss`, `src/css/ds-color-tokens.scss`, `src/css/tokens.scss` |
| Per-component overrides & global CSS (incl. `.ds-toast`) | `src/css/app.scss` |
| Sidebar order / category IA | `.storybook/preview.js` (`storySort`) |
| Amenities (catalog, filter list, popular set) | `src/lib/amenities.js` (single source of truth) |
| Hotel imagery | `src/lib/imagery.js` (+ hosted manifest, see `README.md`) |
| Components | `src/components/` (primitives + `browse/`, `details/`, `checkout/`, `confirmation/`, `managebooking/`) |
| Stories | `src/stories/<category>/*.stories.js` |

## Workflow

1. `pnpm storybook` → open <a href="http://localhost:6006" target="_blank">localhost:6006</a>.
2. Edit any file above — the canvas hot-reloads instantly.
3. Use the **Controls** / **Docs** tabs on each story to QA props and variants.
4. When happy, `pnpm build-storybook` produces a static site in `storybook-static/`.

## Deploy

The build output is a plain static site. Pushing to `main` deploys to GitHub
Pages via `.github/workflows/deploy.yml`. See `README.md` for details.
